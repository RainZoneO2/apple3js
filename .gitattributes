# Normalize all text files to LF in the repository
* text=auto eol=lf

# Binary assets (never touch line endings)
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.glb binary
*.mp3 binary
